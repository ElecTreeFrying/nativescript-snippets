<!-- Svelte flavor (language id: svelte) — Svelte Native component.
     Put the cursor on a blank line inside the markup and type an ns-* prefix
     (e.g. ns-button → <button> — note the LOWERCASE tag; ns-slider-prop → bind:value).
     Svelte has NO icon snippets (ns-icon-* must not appear). -->
<page>
  <stackLayout>

    <!-- type here -->

  </stackLayout>
</page>

<script>
</script>
