// React flavor (language id: typescriptreact) — NativeScript-React (JSX).
// Put the cursor inside the returned JSX and type an ns-* prefix
// (e.g. ns-button → <button></button> — note the LOWERCASE tag).
// React has NO icon snippets (ns-icon-* must not appear).
export function Scratch() {
  return (
    <stackLayout>

      {/* type here */}

    </stackLayout>
  );
}
