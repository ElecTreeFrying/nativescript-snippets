// React flavor (language id: javascriptreact) — same snippets as .tsx.
// Put the cursor inside the returned JSX and type an ns-* prefix
// (e.g. ns-button → <button></button> — LOWERCASE tag).
export function Scratch() {
  return (
    <stackLayout>

      {/* type here */}

    </stackLayout>
  );
}
